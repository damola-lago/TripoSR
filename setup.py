from setuptools import setup

setup(
    name="tsr",
    packages=[
        "tsr",
        "tsr.models",
        "tsr.models.tokenizers",
        "tsr.models.transformer",
    ],
    install_requires=[

    ],
    author="TripoAI",
)